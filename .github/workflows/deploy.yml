name: Deploy to Firebase Hosting

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Create .env from .env.example
        run: cp .env.example .env

      - name: Build
        run: npm run build

      - name: Decode service account
        run: echo '${{ secrets.FIREBASE_SA_BASE64 }}' | base64 -d > /tmp/sa.json

      - name: Verify service account
        run: cat /tmp/sa.json | python3 -c "import sys,json; d=json.load(sys.stdin); print('SA email:', d['client_email']); print('Project:', d['project_id'])"

      - name: Install Firebase Tools
        run: npm install -g firebase-tools

      - name: Check firebase version
        run: firebase --version

      - name: Deploy to Firebase Hosting
        run: firebase deploy --only hosting --project shik-4791f --debug 2>&1
        env:
          GOOGLE_APPLICATION_CREDENTIALS: /tmp/sa.json
